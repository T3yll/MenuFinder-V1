import {{pascalCase name}}Index from '@/modules/{{pascalCase name}}/containers/{{pascalCase name}}Index';
import {{pascalCase name}}Add from '@/modules/{{pascalCase name}}/containers/{{pascalCase name}}Add';
import { Route, Routes } from 'react-router-dom';

const {{pascalCase name}}Router = () => {
  return (
    <Routes>
      <Route path="/" element={<{{pascalCase name}}Index />} />
      <Route path="/add" element={<{{pascalCase name}}Add />} />
      {/* <Route path="/:id" element={} />
      <Route path="/:id/edit" element={} /> */}
    </Routes>
  );
};

export default {{pascalCase name}}Router;